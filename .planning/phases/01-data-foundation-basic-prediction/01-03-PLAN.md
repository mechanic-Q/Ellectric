---
phase: 01-data-foundation-basic-prediction
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - docker-compose.yml
  - notebooks/01_data_ingestion.ipynb
  - notebooks/02_data_cleaning.ipynb
  - notebooks/03_feature_engineering.ipynb
  - notebooks/04_load_forecasting.ipynb
  - notebooks/05_end_to_end_baseline.ipynb
  - README.md
autonomous: true
requirements: [ENV-02]
user_setup:
  - service: Docker
    why: "TimescaleDB + Grafana for Phase 2 visualization (ENV-02). Not required to run Phase 1."
    env_vars: []
    dashboard_config:
      - task: "Install Docker Desktop or Docker Engine (if not already installed)"
        location: "https://docs.docker.com/get-docker/"

must_haves:
  truths:
    - "docker-compose.yml exists in project root with commented-out TimescaleDB and Grafana service definitions"
    - "Every notebook contains markdown explanations before code cells and reflection questions after key visualizations"
    - "README.md includes complete data dictionary for cleaned_load.parquet and features.parquet schemas"
    - "Notebook 01 documents the PUDL data version (v2026.5.0) and Zenodo DOI for reproducibility"
  artifacts:
    - path: "docker-compose.yml"
      provides: "TimescaleDB + Grafana service definitions (commented out for Phase 2)"
      contains: "timescaledb"
    - path: "notebooks/01_data_ingestion.ipynb"
      provides: "Documented data version and DOI"
      contains: "v2026.5.0"
    - path: "README.md"
      provides: "Complete project documentation including data dictionary"
      contains: "## Data Dictionary"
  key_links:
    - from: "docker-compose.yml"
      to: "Phase 2 (ASSUME visualization)"
      via: "commented service definitions"
      pattern: "# Phase 2: Uncomment"
    - from: "notebooks/01_data_ingestion.ipynb"
      to: "Zenodo DOI"
      via: "markdown cell"
      pattern: "10.5281/zenodo.20275549"
---

<objective>
**Polish, Documentation, and Phase 2 Infrastructure** — finalize Phase 1 with Docker Compose configuration for downstream phases, enrich all notebooks with educational content (markdown explanations + reflection questions per ENV-03), document data schemas in README, and pin the PUDL data version with Zenodo DOI for reproducibility.

Purpose: Transform the working notebooks from Plans 01-02 into a polished learning experience. Add the docker-compose.yml skeleton required by ENV-02 (marked as Phase 2 dependency per D-06). Ensure all notebooks meet the ENV-03 standard: "说明(Markdown) → 代码(Cell) → 可视化(Output) → 思考题".

Output: Enhanced docker-compose.yml, polished notebooks (01-05), and updated README.md with complete data dictionary.
</objective>

<execution_context>
@/mnt/e/Ellectric/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Ellectric/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md
@.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md
@.planning/research/STACK.md

<interfaces>
<!-- Existing artifacts this plan polishes -->

Existing notebooks (from Plans 01-02):
- notebooks/01_data_ingestion.ipynb — downloads PUDL data, shows summary
- notebooks/02_data_cleaning.ipynb — cleans data, shows before/after
- notebooks/03_feature_engineering.ipynb — engineers features progressively
- notebooks/04_load_forecasting.ipynb — trains XGBoost, evaluates, visualizes
- notebooks/05_end_to_end_baseline.ipynb — persistence forecast → P&L chart

Existing README.md (from Plan 01):
- Has Quick Start, Phase 1 Overview, Project Structure, Data Source sections

From CONTEXT.md D-06:
Docker Compose optional for Phase 1 — download YAML but mark as "Phase 2 dependency."

From CONTEXT.md ENV-03 requirement:
Jupyter notebooks must include: 说明(Markdown) → 代码(Cell) → 可视化(Output) → 思考题

From RESEARCH.md — PUDL Zenodo DOI:
Concept DOI: 10.5281/zenodo.3653158 (all versions)
Version DOI: 10.5281/zenodo.20275549 (v2026.5.0)

From RESEARCH.md — TIMESCALEDB docker image:
timescale/timescaledb:latest-pg16 (official TimescaleDB Docker image)

From RESEARCH.md — GRAFANA docker image:
grafana/grafana:latest (official Grafana Docker image)

From STACK.md — Docker Compose purpose:
"Optional: docker compose up -d in ASSUME repo for database-backed simulation analysis"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Docker Compose configuration — TimescaleDB + Grafana skeleton for Phase 2</name>
  <files>docker-compose.yml</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md (D-06 — Docker optional for Phase 1, mark as Phase 2 dependency)
    @.planning/research/STACK.md (lines 82-90 — Docker and ASSUME infrastructure context)
    README.md (from Plan 01 — to add Docker section)
  </read_first>
  <action>
    Create docker-compose.yml per ENV-02 with TimescaleDB and Grafana service definitions, ALL commented out per D-06 (not required for Phase 1).

    Write the file with these exact service definitions inside YAML comments (every line starts with `# `):

    ```yaml
    # docker-compose.yml — Ellectric Learning Platform
    # ================================================
    # Phase 2 dependency (ENV-02): TimescaleDB + Grafana for ASSUME market simulation visualization.
    # NOT required for Phase 1. Uncomment and run in Phase 2 after installing ASSUME.
    #
    # Usage (Phase 2):
    #   docker compose up -d
    #   # TimescaleDB available at localhost:5432
    #   # Grafana available at http://localhost:3000 (admin/admin)
    #
    # services:
    #   timescaledb:
    #     image: timescale/timescaledb:latest-pg16
    #     container_name: ellectric-timescaledb
    #     restart: unless-stopped
    #     ports:
    #       - "5432:5432"
    #     environment:
    #       POSTGRES_USER: assume_user
    #       POSTGRES_PASSWORD: assume_password
    #       POSTGRES_DB: assume_db
    #     volumes:
    #       - timescaledb_data:/var/lib/postgresql/data
    #     healthcheck:
    #       test: ["CMD-SHELL", "pg_isready -U assume_user -d assume_db"]
    #       interval: 10s
    #       timeout: 5s
    #       retries: 5
    #
    #   grafana:
    #     image: grafana/grafana:latest
    #     container_name: ellectric-grafana
    #     restart: unless-stopped
    #     ports:
    #       - "3000:3000"
    #     environment:
    #       GF_SECURITY_ADMIN_USER: admin
    #       GF_SECURITY_ADMIN_PASSWORD: admin
    #       GF_INSTALL_PLUGINS: ""
    #     volumes:
    #       - grafana_data:/var/lib/grafana
    #     depends_on:
    #       timescaledb:
    #         condition: service_healthy
    #
    # volumes:
    #   timescaledb_data:
    #   grafana_data:
    ```

    Key decisions embedded:
    - Database name: `assume_db` (matches ASSUME framework expected default)
    - User/password: `assume_user` / `assume_password` (development defaults — documented as such)
    - Grafana admin: `admin` / `admin` (standard Grafana default, learner changes on first login)
    - Healthcheck ensures TimescaleDB is ready before Grafana starts (ASSUME requirement)
    - Volumes are named for data persistence across container restarts

    Do NOT add a `version:` key at the top — Docker Compose v2+ doesn't require it.

    After writing docker-compose.yml, update README.md:
    - Add `## Docker (Phase 2)` section after the Data Source section
    - Content: "Phase 1 runs entirely locally without Docker. The docker-compose.yml file contains commented-out TimescaleDB and Grafana service definitions for Phase 2 market simulation visualization (ASSUME framework). To prepare for Phase 2: `docker compose up -d` (uncomment services first)."
  </action>
  <verify>
    <automated>bash -c 'test -f docker-compose.yml && grep -q "timescaledb" docker-compose.yml && grep -q "grafana" docker-compose.yml && grep -q "Phase 2" docker-compose.yml && echo "docker-compose.yml OK"'</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml exists at project root with TimescaleDB and Grafana service definitions
    - ALL service definitions are commented out (every line starts with `# `)
    - File includes Phase 2 usage instructions in comments
    - README.md has a "Docker (Phase 2)" section explaining the file is for Phase 2 only
    - TimescaleDB uses timescale/timescaledb:latest-pg16 image with assume_user/assume_password credentials
    - Grafana uses grafana/grafana:latest image with admin/admin credentials
    - Healthcheck ensures proper service startup order (TimescaleDB before Grafana)
  </acceptance_criteria>
  <done>docker-compose.yml committed. Phase 2 infrastructure is defined and documented as a future dependency.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Notebook polish — markdown explanations, reflection questions, data version documentation per ENV-03</name>
  <files>notebooks/01_data_ingestion.ipynb, notebooks/02_data_cleaning.ipynb, notebooks/03_feature_engineering.ipynb, notebooks/04_load_forecasting.ipynb, notebooks/05_end_to_end_baseline.ipynb</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md (ENV-03 requirement: 说明→代码→可视化→思考题; D-02 — version pinning)
    notebooks/01_data_ingestion.ipynb (from Plan 01 — review current state)
    notebooks/02_data_cleaning.ipynb (from Plan 01 — review current state)
    notebooks/03_feature_engineering.ipynb (from Plan 02 — review current state)
    notebooks/04_load_forecasting.ipynb (from Plan 02 — review current state)
    notebooks/05_end_to_end_baseline.ipynb (from Plan 01 — review current state)
  </read_first>
  <action>
    Polish all five notebooks to meet ENV-03 standard: every notebook follows the pattern 说明(Markdown) → 代码(Cell) → 可视化(Output) → 思考题(Reflection).

    **For each notebook, read its current content, then enhance with these additions:**

    **01_data_ingestion.ipynb:**
    - Add after the first markdown cell: explanation of what PUDL is (Catalyst Cooperative, CC-BY-4.0, cleans US EIA data), what EIA-930 measures (hourly electricity demand by balancing authority), and why PJM (largest US RTO, 65M+ people served)
    - Add after data download code: markdown cell with "## Data Version" documenting PUDL v2026.5.0, Zenodo DOI 10.5281/zenodo.20275549, and why version pinning matters (reproducibility across learners)
    - Add after data summary cells: markdown cell explaining imputed vs raw demand (demand_mwh vs demand_imputed_mwh columns)
    - Add as final cell: markdown "## Reflection" with question: "PUDL provides both `demand_mwh` (raw reported) and `demand_imputed_mwh` (gap-filled). Which should we use for load forecasting? Why?" and "What date range does the data cover? How many years of PJM demand history do we have?"

    **02_data_cleaning.ipynb:**
    - Add after missing-value report output: markdown cell explaining why some hours have missing demand (meter errors, communication failures) and why IQR outlier detection is report-only (outliers may be real extreme load events, not errors — per Pitfall 2: spikes ARE the signal)
    - Add after UTC verification: markdown cell "## Why UTC?" explaining: PJM operates in US Eastern timezone but UTC avoids DST transition issues (23h and 25h days). All downstream features and predictions use UTC. Calendar features (hour, day-of-week) are computed from UTC timestamps.
    - Add after load plot: markdown "## Reflection" with questions: "What percentage of hours had missing load values? If we removed IQR outliers instead of just reporting them, what extreme events might we lose?" and "Why do we keep timestamps in UTC instead of converting to Eastern time?"

    **03_feature_engineering.ipynb:**
    - Add after feature descriptions: markdown cell "## Why Progressive Features?" explaining D-07: start simple (5 core features), measure model performance, then add complexity. Each new feature must earn its place by improving metrics.
    - Add before lag-24h scatter: markdown cell explaining autocorrelation: "Electricity load at hour H is strongly correlated with load at hour H-24 (same hour yesterday) and H-168 (same hour last week). These lag features capture daily and weekly patterns without look-ahead bias because .shift() only uses past data."
    - Add before rolling stats: markdown cell explaining closed='left': "Rolling statistics use `shift(1).rolling(closed='left')` — this ensures the current hour's value is NOT included in its own rolling window. Without this, the model would 'cheat' by seeing the answer in the feature."
    - Add after correlation heatmap: markdown "## Reflection" with questions: "Which feature has the highest correlation with load_mw? Why does lag-24h dominate over lag-168h?" and "What would happen if we computed rolling_mean_24h without .shift(1) first?"

    **04_load_forecasting.ipynb:**
    - Add after fold metrics table: markdown cell explaining what each fold represents: "TimeSeriesSplit creates 5 expanding windows. Fold 1 trains on the earliest data, validates on the next chunk. Fold 5 trains on most of the data. Cross-fold MAE should decrease as training data grows — if it increases, the model may be overfitting."
    - Add after test metrics: markdown "## Understanding the Metrics" with bullet definitions:
      - MAE (Mean Absolute Error): average error in MW — easy to interpret
      - RMSE (Root Mean Squared Error): penalizes large errors more heavily
      - MAPE (Mean Absolute Percentage Error): scale-independent, expressed as %
      - R² (Coefficient of Determination): 1.0 = perfect, 0.0 = mean predictor, negative = worse than mean
    - Add explanation of why MAPE <5% is a reasonable target for PJM load forecasting with basic features
    - Add after residuals plot: markdown cell "## Diagnosing Model Problems" with guidance:
      - "If residuals show a pattern (e.g., positive then negative), the model is missing a systematic signal — add features."
      - "If errors are largest at specific hours (e.g., morning ramp), those hours need targeted features."
      - "If errors grow over time, the model may be concept-drifted — retrain on more recent data."
    - Add final markdown: "## Reflection" with questions: "How does XGBoost MAE compare to persistence MAE from notebook 05? Is the improvement meaningful?" and "At which hours does XGBoost make the largest errors? What features might reduce those errors?"

    **05_end_to_end_baseline.ipynb:**
    - Add after P&L chart: markdown cell "## Interpreting the P&L Chart" explaining:
      - Negative cumulative P&L is EXPECTED for a naive persistence forecast — the purpose is integration proof, not profitability
      - A flat price assumption ($50/MWh) is a simplification — real electricity prices vary by hour
      - The downward trend shows that forecast errors compound: the worse the forecast, the more money is lost
    - Add after monthly P&L: markdown cell explaining seasonality: "Different months may show different P&L patterns due to seasonal load changes. Winter and summer peaks are harder to predict than shoulder months."
    - Add final markdown: "## Reflection" with questions: "If you had a perfect forecast (error = 0), what would the P&L chart look like?" and "The persistence forecast loses money — how much better does XGBoost need to be to break even?" and "What's the NEXT step? Open notebook 04 to train a real model, then come back and compare P&L charts."

    **General polish for all notebooks:**
    - Ensure every notebook's first cell is `%matplotlib inline` (if not already)
    - Ensure no cell produces excessive output (>50 lines) — use .head() or .describe() for DataFrames
    - Add a "Notebook Order" markdown cell at the TOP of each notebook (after the title) listing: "📍 You are here: 01 → 02 → 03 → 04 → 05" with the current notebook highlighted
    - Document the Zenodo DOI (10.5281/zenodo.20275549) in notebook 01's data version section per D-02

    Per D-09: all production logic remains in pipeline/*.py modules. Notebooks are education + visualization layers.
  </action>
  <verify>
    <automated>python3.11 -c "
import json
required_sections = {
    '01_data_ingestion': ['PUDL', 'v2026.5.0', 'Zenodo'],
    '02_data_cleaning': ['UTC', 'IQR', 'outlier'],
    '03_feature_engineering': ['lag', 'shift', 'rolling'],
    '04_load_forecasting': ['MAE', 'RMSE', 'MAPE', 'TimeSeriesSplit'],
    '05_end_to_end_baseline': ['persistence', 'P&amp;L', 'cumulative']
}
for nb_name, keywords in required_sections.items():
    with open(f'notebooks/{nb_name}.ipynb') as f:
        content = f.read()
    missing = [kw for kw in keywords if kw.lower() not in content.lower()]
    if missing:
        print(f'{nb_name}: MISSING keywords: {missing}')
    else:
        print(f'{nb_name}: ALL keywords present')
print('Notebook polish verification complete')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every notebook (01-05) follows the pattern: markdown explanation → code cell → visualization → reflection questions (ENV-03)
    - Notebook 01 documents PUDL data version (v2026.5.0) and Zenodo DOI (10.5281/zenodo.20275549)
    - Notebook 02 explains why UTC (not Eastern time) and why IQR outliers are reported but not removed
    - Notebook 03 explains progressive feature design, autocorrelation via lag features, and closed='left' rolling
    - Notebook 04 explains cross-fold interpretation, metric definitions (MAE/RMSE/MAPE/R²), and model diagnosis guidance
    - Notebook 05 explains P&L chart interpretation and the purpose of the persistence baseline
    - All notebooks have a "Notebook Order" navigation cell at the top
    - No notebook cell produces excessive output; DataFrames use .head() or .describe()
  </acceptance_criteria>
  <done>All notebooks polished with educational content. Learners follow a guided tutorial with explanations and reflection questions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: README data dictionary — complete schema documentation for all Phase 1 output files</name>
  <files>README.md</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md (lines 324-345 — Parquet data contracts: cleaned_load.parquet schema and features.parquet/forecast.parquet schemas)
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md (D-04 — standardized column schema)
    README.md (from Plan 01 — existing content to enhance)
  </read_first>
  <action>
    Enhance README.md with comprehensive data dictionary and final project polish.

    **Add after "Project Structure" section: `## Data Dictionary`**

    Document every Phase 1 output file with its schema:

    ```markdown
    ## Data Dictionary

    ### cleaned_load.parquet (output of notebook 02)
    Cleaned PJM hourly demand data. Source: PUDL EIA-930, filtered to PJM balancing authority.

    | Column | Type | Description |
    |--------|------|-------------|
    | `timestamp` | datetime64[ns, UTC] | Hourly time index, UTC timezone |
    | `load_mw` | float64 | PJM system load in megawatts (from demand_imputed_mwh) |
    | `subregion` | str | PJM subregion identifier (e.g., "PJM RTO") |

    ### features.parquet (output of notebook 03)
    Feature-engineered dataset ready for model training. All calendar features derived from UTC timestamp.

    | Column | Type | Description |
    |--------|------|-------------|
    | `timestamp` | datetime64[ns, UTC] | Hourly time index |
    | `load_mw` | float64 | Target variable — PJM system load |
    | `subregion` | str | PJM subregion identifier |
    | `hour` | int8 | Hour of day (0-23, UTC) |
    | `day_of_week` | int8 | Day of week (0=Monday, 6=Sunday) |
    | `month` | int8 | Month (1-12) |
    | `is_weekend` | bool | True if Saturday or Sunday |
    | `is_holiday` | bool | True if US federal holiday |
    | `load_lag_24h` | float64 | Load 24 hours ago (same hour yesterday) |
    | `load_lag_168h` | float64 | Load 168 hours ago (same hour last week) |
    | `load_rolling_mean_24h` | float64 | 24-hour rolling mean (excludes current hour) |
    | `load_rolling_std_24h` | float64 | 24-hour rolling standard deviation (excludes current hour) |

    ### forecast.parquet (output of notebook 04)
    XGBoost load forecast with ground truth for evaluation.

    | Column | Type | Description |
    |--------|------|-------------|
    | `timestamp` | datetime64[ns, UTC] | Forecast target hour |
    | `actual_load_mw` | float64 | Ground truth load (MW) |
    | `predicted_load_mw` | float64 | XGBoost predicted load (MW) |

    ### xgboost_load_model.json (output of notebook 04)
    Trained XGBoost model in native JSON format. Loadable via `xgb.XGBRegressor().load_model()`.
    ```

    **Add after Data Dictionary: `## Data Source` section** (move existing source info here):
    ```markdown
    ## Data Source

    **PUDL (Public Utility Data Liberation Project)**
    - Organization: Catalyst Cooperative
    - License: CC-BY-4.0
    - Version: v2026.5.0 (released 2026-05-18)
    - Zenodo DOI: [10.5281/zenodo.20275549](https://doi.org/10.5281/zenodo.20275549)
    - Table used: `out_eia930__hourly_subregion_demand`
    - Filter: `balancing_authority_code == "PJM"`
    - Access: Public S3 bucket (no credentials required)
    ```

    **Add after Docker section: `## Notebook Guide`**
    ```markdown
    ## Notebook Guide

    Phase 1 notebooks are designed to be executed in order. Each builds on the previous.

    | # | Notebook | What You'll Do | Key Output |
    |---|----------|----------------|------------|
    | 01 | `01_data_ingestion.ipynb` | Download PJM demand data from PUDL S3 | `data/raw/pjm_demand.parquet` |
    | 02 | `02_data_cleaning.ipynb` | Clean data: missing values, IQR outliers, UTC | `data/processed/cleaned_load.parquet` |
    | 03 | `03_feature_engineering.ipynb` | Engineer features: calendar, lag, rolling | `data/processed/features.parquet` |
    | 04 | `04_load_forecasting.ipynb` | Train XGBoost, evaluate, visualize | `models/xgboost_load_model.json` |
    | 05 | `05_end_to_end_baseline.ipynb` | End-to-end: persistence → P&L chart | Integration proof |
    ```

    **Add at the very end: `## Next Steps`**
    ```markdown
    ## Next Steps

    After completing Phase 1, you've proven the full data→predict→trade pipeline works. In Phase 2:
    - Replace manual XGBoost with OpenSTEF automated forecasting
    - Add electricity price forecasting with epftoolbox
    - Run ASSUME market simulations with Grafana dashboards

    → Continue to Phase 2: Deep Prediction + Market Simulation
    ```

    Ensure the existing "Quick Start" and "Project Structure" sections remain intact and correct.
  </action>
  <verify>
    <automated>bash -c 'grep -q "Data Dictionary" README.md && grep -q "cleaned_load.parquet" README.md && grep -q "features.parquet" README.md && grep -q "forecast.parquet" README.md && grep -q "Zenodo DOI" README.md && grep -q "Notebook Guide" README.md && grep -q "Next Steps" README.md && echo "README data dictionary OK"'</automated>
  </verify>
  <acceptance_criteria>
    - README.md has a "Data Dictionary" section documenting schemas for cleaned_load.parquet (3 columns), features.parquet (12 columns), forecast.parquet (3 columns), and xgboost_load_model.json
    - README.md has a "Data Source" section with PUDL version, Zenodo DOI, license, and access method
    - README.md has a "Notebook Guide" table listing all 5 notebooks with descriptions and key outputs
    - README.md has a "Next Steps" section pointing to Phase 2
    - All column names, types, and descriptions match the actual implementations
    - Zenodo DOI (10.5281/zenodo.20275549) is correctly cited
  </acceptance_criteria>
  <done>README.md updated with complete data dictionary, notebook guide, and Phase 2 pointer. Phase 1 documentation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker images (timescale/timescaledb, grafana/grafana) | External container images from Docker Hub — supply chain risk for Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | docker-compose.yml — container image tags | mitigate | Services are commented out in Phase 1 (D-06). When uncommented in Phase 2, pin exact image digests (not :latest) after first pull. Currently using :latest tags for skeleton only — documented as development defaults. |
| T-03-02 | Elevation of Privilege | docker-compose.yml — container privileges | mitigate | No privileged mode, no host network, no volume mounts beyond named Docker volumes and explicit data paths. TimescaleDB and Grafana run as their default non-root users. |
| T-03-03 | Information Disclosure | docker-compose.yml — credentials in file | accept | Default credentials (assume_user/assume_password, admin/admin) are documented as development-only. Single-user local environment. Phase 2 will add .env file support for credential management. |
</threat_model>

<verification>
1. Verify docker-compose.yml exists with both services defined (all commented out)
2. Verify all 5 notebooks open in Jupyter and display markdown explanations + reflection questions
3. Verify each notebook contains a "Notebook Order" navigation cell
4. Verify README.md Data Dictionary tables match actual column schemas in the code
5. Verify Zenodo DOI appears in both notebook 01 markdown and README.md Data Source section
6. Verify README.md Next Steps section references Phase 2
</verification>

<success_criteria>
- [ ] docker-compose.yml has commented-out TimescaleDB + Grafana with healthcheck and volume persistence
- [ ] All 5 notebooks follow the 说明→代码→可视化→思考题 pattern (ENV-03)
- [ ] Data version (v2026.5.0) and Zenodo DOI documented in notebook 01 and README
- [ ] README.md includes complete data dictionary for all 4 output file types
- [ ] Notebook Guide table and Next Steps section exist in README
- [ ] Phase 2 is clearly referenced as the next step without starting Phase 2 work
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-basic-prediction/01-03-SUMMARY.md` when done
</output>
