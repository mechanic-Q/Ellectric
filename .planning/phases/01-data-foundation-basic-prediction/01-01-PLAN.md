---
phase: 01-data-foundation-basic-prediction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - setup.sh
  - .gitignore
  - README.md
  - pipeline/__init__.py
  - pipeline/data_loader.py
  - pipeline/cleaner.py
  - notebooks/01_data_ingestion.ipynb
  - notebooks/02_data_cleaning.ipynb
  - notebooks/05_end_to_end_baseline.ipynb
autonomous: false
requirements: [ENV-01, ENV-03, DATA-01, DATA-03]
user_setup: []

must_haves:
  truths:
    - "User runs setup.sh on a clean Python 3.11 machine and all core dependencies (pandas, scikit-learn, xgboost, matplotlib, jupyter) are importable"
    - "User executes notebook 01_data_ingestion.ipynb and sees PJM hourly demand data downloaded with row counts and column summaries"
    - "User executes notebook 02_data_cleaning.ipynb and sees missing-value report, IQR outlier counts, and UTC-confirmation output"
    - "User finds cleaned_load.parquet in data/processed/ with documented timestamp, load_mw, and subregion columns"
    - "User executes notebook 05_end_to_end_baseline.ipynb and sees a cumulative P&L chart rendering from the persistence forecast"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned Python dependencies"
      contains: "pandas==3.0.3"
    - path: "setup.sh"
      provides: "One-command environment bootstrap"
      contains: "python3.11 -m venv"
    - path: ".gitignore"
      provides: "Excludes data artifacts and venv from git"
      contains: "data/raw/"
    - path: "pipeline/data_loader.py"
      provides: "PUDL data download from S3"
      exports: ["download_pjm_demand"]
    - path: "pipeline/cleaner.py"
      provides: "Missing-value and IQR-outlier detection, UTC normalization"
      exports: ["clean_pjm_data"]
    - path: "notebooks/05_end_to_end_baseline.ipynb"
      provides: "End-to-end persistence forecast + P&L chart"
      contains: "persistence_forecast"
  key_links:
    - from: "setup.sh"
      to: "requirements.txt"
      via: "pip install -r"
      pattern: "pip install -r requirements.txt"
    - from: "notebooks/01_data_ingestion.ipynb"
      to: "pipeline/data_loader.py"
      via: "import pipeline.data_loader"
      pattern: "from pipeline.data_loader import download_pjm_demand"
    - from: "notebooks/05_end_to_end_baseline.ipynb"
      to: "data/processed/cleaned_load.parquet"
      via: "pd.read_parquet"
      pattern: "pd.read_parquet.*cleaned_load"
    - from: "notebooks/05_end_to_end_baseline.ipynb"
      to: "matplotlib cumulative P&L chart"
      via: "plt.plot"
      pattern: "plt.plot.*cumulative_pnl"
---

<objective>
**Walking Skeleton** — the thinnest possible end-to-end slice proving the full data→predict→trade pipeline connects.

Purpose: Deliver a working Jupyter environment where the learner can: (1) install all dependencies with one command, (2) download PJM hourly demand data from PUDL S3, (3) run the cleaning pipeline producing validated Parquet output, and (4) execute a persistence forecast through a minimal P&L calculation yielding a cumulative profit chart — all within the first session.

Output: Project scaffold (requirements.txt, setup.sh, .gitignore, README.md, directory tree), two pipeline modules (data_loader.py, cleaner.py), and three Jupyter notebooks (01, 02, 05) that together exercise the full stack. Plus SKELETON.md recording architectural decisions for subsequent phases.
</objective>

<execution_context>
@/mnt/e/Ellectric/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Ellectric/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md
@.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from research artifacts. -->

From RESEARCH.md — PUDL EIA-930 table schema (out_eia930__hourly_subregion_demand.parquet):
Columns: timestamp (datetime64[ns, UTC]), balancing_authority_code (str), subregion (str), demand_mwh (float64), demand_imputed_mwh (float64)

From RESEARCH.md — cleaned_load.parquet output schema:
Columns: timestamp (datetime64[ns, UTC]), load_mw (float64), subregion (str)

From CONTEXT.md D-04 — Standardized column schema:
datetime_utc (timezone-aware), load_mw (float64), temperature_f (float64, if weather available), plus metadata columns. All timestamps in UTC.

From CONTEXT.md D-09 — Module pattern:
Notebooks import from pipeline/*.py modules using `from pipeline.data_loader import download_pjm_demand`.
Notebooks are thin wrappers; all production logic lives in .py modules.

From RESEARCH.md — HTTPS download URL (primary, no credentials needed):
https://s3.us-west-2.amazonaws.com/pudl.catalyst.coop/v2026.5.0/out_eia930__hourly_subregion_demand.parquet

From RESEARCH.md — PUDL BA code filter:
balancing_authority_code == "PJM" to filter to PJM region only.

From CONTEXT.md D-05 — Environment setup command:
python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

From CONTEXT.md D-10 — Notebook naming:
01_data_ingestion.ipynb, 02_data_cleaning.ipynb, 05_end_to_end_baseline.ipynb

From RESEARCH.md — Package versions (all PyPI-verified):
pandas==3.0.3, numpy==2.3.5, pyarrow==22.0.0, scikit-learn==1.8.0, xgboost==3.2.0, matplotlib==3.10.9, seaborn==0.13.2, jupyter==1.1.1, holidays==0.71

From RESEARCH.md — Do NOT install: catalystcoop.pudl (version doesn't exist on PyPI), enda (H2O dependency conflict)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Project scaffold — directory tree, requirements.txt, setup.sh, .gitignore, README.md</name>
  <files>requirements.txt, setup.sh, .gitignore, README.md, pipeline/__init__.py</files>
  <read_first>
    @.planning/research/STACK.md (lines 29-38 — Phase 1 stack pattern: pandas + scikit-learn + xgboost + matplotlib)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md (lines 89-112 — exact requirements.txt content and pinned versions)
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md (D-05 — setup command, D-06 — Docker optional)
  </read_first>
  <action>
    Create the project skeleton:

    1. Create directory tree (empty dirs with placeholder .gitkeep in data/raw/ and data/processed/):
       data/raw/.gitkeep, data/processed/.gitkeep, pipeline/__init__.py, notebooks/, models/

    2. Write requirements.txt with these exact pinned versions (one per line, grouped with comments):
       # Core data science
       pandas==3.0.3
       numpy==2.3.5
       pyarrow==22.0.0
       # Machine learning
       scikit-learn==1.8.0
       xgboost==3.2.0
       # Visualization
       matplotlib==3.10.9
       seaborn==0.13.2
       # Notebook environment
       jupyter==1.1.1
       # Feature engineering support
       holidays==0.71

       Do NOT include: catalystcoop.pudl, enda, openstef, assume-framework, fastapi, langchain, or any library not on the list above.

    3. Write setup.sh (executable, shebang #!/usr/bin/env bash):
       - Check for Python 3.11+ with `python3.11 --version`
       - Print error and exit 1 if python3.11 not found
       - Create venv: `python3.11 -m venv .venv`
       - Print activation instructions
       - Install: `.venv/bin/pip install -r requirements.txt`
       - Print success message with "jupyter notebook" launch hint
       - Handle failures: if pip install fails, print the error and exit with pip's exit code

    4. Write .gitignore covering: .venv/, data/raw/*.parquet, data/processed/*.parquet, __pycache__/, *.pyc, .ipynb_checkpoints/, *.egg-info/, dist/, .DS_Store

    5. Write README.md with sections:
       - # Ellectric: AI + Electricity Trading Learning Platform
       - ## Quick Start (setup.sh instructions)
       - ## Phase 1 Overview (what this phase delivers)
       - ## Project Structure (directory tree)
       - ## Data Source (PUDL v2026.5.0, PJM interconnection)

    6. Write pipeline/__init__.py as empty file with docstring: `"""Ellectric data pipeline modules."""`
  </action>
  <verify>
    <automated>bash -c 'test -f requirements.txt && grep -c "pandas==3.0.3" requirements.txt && test -f setup.sh && test -x setup.sh && test -f .gitignore && grep -q "data/raw/" .gitignore && test -f README.md && test -f pipeline/__init__.py && test -d data/raw && test -d data/processed && test -d notebooks && test -d models'</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt exists with exactly 9 pinned packages (pandas, numpy, pyarrow, scikit-learn, xgboost, matplotlib, seaborn, jupyter, holidays) and no extra packages
    - setup.sh is executable, checks for python3.11, and exits 1 if not found
    - .gitignore covers .venv/, data/raw/*.parquet, data/processed/*.parquet, __pycache__/, *.pyc, .ipynb_checkpoints/
    - README.md exists with Quick Start, Phase 1 Overview, Project Structure, and Data Source sections
    - All directories (data/raw, data/processed, pipeline, notebooks, models) exist
    - pipeline/__init__.py exists with docstring
  </acceptance_criteria>
  <done>Project scaffold committed. setup.sh can be run to bootstrap a clean Python 3.11 environment.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Data pipeline — PUDL download module + cleaning module + Parquet output</name>
  <files>pipeline/data_loader.py, pipeline/cleaner.py</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md (lines 467-519 — download_pjm_demand reference implementation; lines 294-334 — scaler-on-train-only pattern for reference)
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md (D-01 — PJM data, D-03 — Parquet format, D-04 — column schema)
    @.planning/research/PITFALLS.md (lines 13-63 — Pitfall 1: look-ahead bias background for awareness)
  </read_first>
  <action>
    Write two pipeline modules implementing the data layer per D-09 (production logic in .py modules, notebooks are thin wrappers):

    **pipeline/data_loader.py** — single function `download_pjm_demand()`:
    - Signature: `download_pjm_demand(output_path: str = "data/raw/pjm_demand.parquet", force: bool = False) -> pd.DataFrame`
    - Uses HTTPS direct download (NOT S3 protocol, NOT boto3/AWS CLI) from:
      `https://s3.us-west-2.amazonaws.com/pudl.catalyst.coop/v2026.5.0/out_eia930__hourly_subregion_demand.parquet`
    - Implements caching: if output_path exists and force=False, reads from cache with `pd.read_parquet(output_path)` and returns
    - On download: `df = pd.read_parquet(table_url)` (pandas 3.0.3 uses pyarrow backend)
    - Filter to PJM only: `df_pjm = df[df["balancing_authority_code"] == "PJM"].copy()`
    - Keep columns: timestamp, subregion, demand_mwh, demand_imputed_mwh
    - Ensure output directory exists with `Path(output_path).parent.mkdir(parents=True, exist_ok=True)`
    - Save with `df_pjm.to_parquet(output_path, index=False)`
    - Print progress messages: "Downloading PUDL EIA-930 data from {url}...", "Saved {rows:,} rows to {path}"
    - Return the DataFrame
    - Module-level constants: `PUDL_S3_BASE = "https://s3.us-west-2.amazonaws.com/pudl.catalyst.coop"`, `DATA_VERSION = "v2026.5.0"`, `PJM_BA_CODE = "PJM"`
    - Add docstring documenting the PUDL table source, version, and filter logic
    - Handle errors: if the URL is unreachable, raise a descriptive RuntimeError with the URL and troubleshooting hints (check internet, try Zenodo alternative)

    **pipeline/cleaner.py** — single function `clean_pjm_data()`:
    - Signature: `clean_pjm_data(df: pd.DataFrame, output_path: str = "data/processed/cleaned_load.parquet") -> pd.DataFrame`
    - Uses demand_imputed_mwh column as primary load value (imputed fills gaps; per RESEARCH.md open question recommendation)
    - Renames: demand_imputed_mwh → load_mw
    - Ensures timestamp is timezone-aware UTC: `pd.to_datetime(df["timestamp"], utc=True)`
    - Sets timestamp as DataFrame index (sorted ascending), then resets it as a column at the end
    - Missing value report: prints count of NaN values per column BEFORE any handling
    - IQR outlier detection (report-only, per DATA-03 — do NOT remove outliers):
      - Calculate Q1, Q3, IQR for load_mw
      - Print: "Outlier detection (IQR method): {count} values outside [{lower_bound:.1f}, {upper_bound:.1f}] MW"
    - Drops rows where load_mw is NaN (after reporting count)
    - Validates: assert that no NaN values remain in load_mw after dropping
    - Validates: assert that timestamp dtype is datetime64[ns, UTC]
    - Output schema exactly: timestamp (datetime64[ns, UTC]), load_mw (float64), subregion (str)
    - Saves to output_path as Parquet with `to_parquet(output_path, index=False)`
    - Returns the cleaned DataFrame
    - Add docstring documenting the cleaning steps and output schema

    Per D-04: All timestamps remain in UTC. No timezone conversion to local time.
    Per D-03: All outputs are Parquet files. No CSV, no SQLite.
    Per D-09: These are production .py modules — no notebook-specific code, no hardcoded display paths.
  </action>
  <verify>
    <automated>python3.11 -c "from pipeline.data_loader import download_pjm_demand; from pipeline.cleaner import clean_pjm_data; print('Imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/data_loader.py contains function download_pjm_demand with PUDL_S3_BASE, DATA_VERSION, PJM_BA_CODE constants
    - download_pjm_demand uses HTTPS URL (not s3:// protocol), filters for balancing_authority_code == "PJM", and caches to data/raw/pjm_demand.parquet
    - pipeline/cleaner.py contains function clean_pjm_data that renames demand_imputed_mwh to load_mw, enforces UTC timezone, reports missing values and IQR outliers (without removing outliers), drops NaN load rows, outputs to data/processed/cleaned_load.parquet
    - clean_pjm_data validates output: no NaN in load_mw, timestamp is datetime64[ns, UTC]
    - Both modules are importable without errors (verify command passes)
    - Neither module depends on enda, catalystcoop.pudl, or any library not in requirements.txt
  </acceptance_criteria>
  <done>Data pipeline modules committed. PUDL data can be downloaded and cleaned via importable .py functions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: End-to-end baseline notebook — persistence forecast + P&L chart proving full pipeline connects</name>
  <files>notebooks/01_data_ingestion.ipynb, notebooks/02_data_cleaning.ipynb, notebooks/05_end_to_end_baseline.ipynb</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md (lines 576-628 — persistence_forecast and calculate_baseline_pnl reference implementation; lines 346-367 — thin-notebook pattern)
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md (D-11 — persistence baseline, D-12 — P&L chart verification, D-14 — visualization minimum)
    @.planning/research/PITFALLS.md (lines 233-276 — Pitfall 5: run end-to-end early)
    pipeline/data_loader.py (created by Task 2 — read for function signatures)
    pipeline/cleaner.py (created by Task 2 — read for function signatures)
  </read_first>
  <action>
    Create three Jupyter notebooks per D-10 naming convention. All notebooks import from pipeline/*.py modules per D-09 (thin wrappers + markdown + visualization).

    **notebooks/01_data_ingestion.ipynb:**
    - Markdown cell: "# 01: Data Ingestion — PJM Hourly Demand from PUDL" with explanation of PUDL source, EIA-930 table, PJM as largest US RTO, data version v2026.5.0
    - Code cell: `from pipeline.data_loader import download_pjm_demand` then `df = download_pjm_demand()`
    - Code cell: `df.head(10)` displaying first rows
    - Code cell: `df.info()` showing dtypes and non-null counts
    - Code cell: `df.describe()` showing load_mw summary statistics
    - Code cell: print shape, date range (min/max timestamp), unique subregions
    - Markdown cell: "## Next: Data Cleaning → open `02_data_cleaning.ipynb`" with reflection question: "What did you notice about the data shape? How many rows per subregion?"

    **notebooks/02_data_cleaning.ipynb:**
    - Markdown cell: "# 02: Data Cleaning — Missing Values, Outliers, UTC Normalization"
    - Code cell: `from pipeline.data_loader import download_pjm_demand; from pipeline.cleaner import clean_pjm_data`
    - Code cell: Load raw: `df_raw = download_pjm_demand()`
    - Code cell: Run cleaning: `df_clean = clean_pjm_data(df_raw)` — function prints missing-value report and IQR outlier counts automatically
    - Code cell: `df_clean.head(10)` showing cleaned data
    - Code cell: Verify schema: print column names, dtypes, confirm timestamp is tz-aware UTC, confirm no NaN in load_mw
    - Code cell: Quick plot: `df_clean.set_index("timestamp")["load_mw"].plot(title="PJM Hourly Demand (MW)", figsize=(14, 4))`
    - Markdown cell: "## Next: Feature Engineering → Phase 2 notebook" with reflection: "How many rows had missing load values? What percentage of total is that?"

    **notebooks/05_end_to_end_baseline.ipynb** — the critical integration proof:
    - Markdown cell: "# 05: End-to-End Baseline — Persistence Forecast → P&L"
    - Markdown cell: "## Why This Notebook Exists" explaining Pitfall 5: run end-to-end EARLY, before model tuning. "This notebook uses the simplest possible forecast (yesterday = today) to prove the data→predict→trade pipeline connects end-to-end."
    - Code cell: imports — `pandas as pd`, `numpy as np`, `matplotlib.pyplot as plt`, `from pathlib import Path`
    - Code cell: Load cleaned data: `df = pd.read_parquet("../../data/processed/cleaned_load.parquet")`
    - Markdown cell: "## Step 1: Persistence Forecast (yesterday = today)"
    - Code cell: implement `persistence_forecast()` inline (not from external module — this notebook is self-contained for the baseline):
      - Copy df[["timestamp", "load_mw"]], set timestamp as index
      - Create predicted_load_mw = load_mw.shift(24) (yesterday same hour = today's prediction)
      - Drop NaN rows (first 24 hours have no lag)
      - Print: rows in forecast, date range, first/last prediction
    - Code cell: Overlay plot — `plt.plot(forecast.index[:168], forecast["load_mw"][:168], label="Actual")` and `plt.plot(forecast.index[:168], forecast["predicted_load_mw"][:168], label="Persistence (t-24h)", alpha=0.7)` for first 7 days
    - Markdown cell: "## Step 2: Minimal P&L Calculation"
    - Code cell: implement `calculate_baseline_pnl()` inline per D-11:
      - Flat price assumption: PRICE_PER_MWH = 50.0 (documented as assumption)
      - error_mw = actual - predicted
      - pnl_per_hour = -abs(error_mw) * PRICE_PER_MWH (simplified model: deviation costs money)
      - cumulative_pnl = pnl_per_hour.cumsum()
      - Return DataFrame with error_mw, pnl_per_hour, cumulative_pnl columns
    - Code cell: Run calculation, print summary: total P&L, avg hourly P&L, hours positive/negative
    - Markdown cell: "## Step 3: Cumulative P&L Chart" (per D-12)
    - Code cell: `plt.figure(figsize=(14, 5))` with `plt.plot(results["cumulative_pnl"])`, title "Cumulative P&L: Persistence Forecast Baseline", xlabel "Hour", ylabel "Cumulative P&L ($)", grid=True
    - Code cell: Monthly aggregation: group by month, sum pnl_per_hour, bar chart showing monthly P&L
    - Markdown cell: "## What This Proves" with bullet list: (a) Data pipeline → forecast → trade → P&L all connect, (b) Column schemas are compatible, (c) P&L logic is correct, (d) The pipeline is ready for XGBoost model in notebook 04
    - Markdown cell: "## Reflection" with question: "The persistence forecast loses money (negative P&L). What would a 'perfect' forecast do to the P&L chart? How much better does a real model need to be?"

    Per D-14: notebooks 01 and 02 include basic matplotlib plots (load time series). Notebook 05 includes: (a) load-vs-prediction overlay for first week, (b) P&L per hour visualization, (c) cumulative P&L chart, (d) monthly P&L bar chart.

    All notebooks: use `%matplotlib inline` in first code cell. Use `plt.tight_layout()` before `plt.show()`. Set `plt.style.use('seaborn-v0_8-whitegrid')` or default matplotlib style.
  </action>
  <verify>
    <automated>python3.11 -c "
import json
for nb in ['01_data_ingestion', '02_data_cleaning', '05_end_to_end_baseline']:
    with open(f'notebooks/{nb}.ipynb') as f:
        data = json.load(f)
    code_cells = [c for c in data['cells'] if c['cell_type'] == 'code']
    md_cells = [c for c in data['cells'] if c['cell_type'] == 'markdown']
    print(f'{nb}: {len(code_cells)} code cells, {len(md_cells)} markdown cells')
# Verify 05 contains the key function names
with open('notebooks/05_end_to_end_baseline.ipynb') as f:
    content = f.read()
assert 'persistence_forecast' in content, 'Missing persistence_forecast in 05'
assert 'cumulative_pnl' in content, 'Missing cumulative_pnl in 05'
assert 'PRICE_PER_MWH' in content, 'Missing PRICE_PER_MWH in 05'
print('All notebook checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - notebooks/01_data_ingestion.ipynb exists, imports from pipeline.data_loader, displays data summary (head, info, describe, date range)
    - notebooks/02_data_cleaning.ipynb exists, imports from pipeline.cleaner, displays before/after stats, includes load time-series plot
    - notebooks/05_end_to_end_baseline.ipynb exists with persistence_forecast (shift(24)), calculate_baseline_pnl (flat $50/MWh price, negative-absolute-error model), cumulative P&L chart, and monthly P&L bar chart
    - Notebook 05 executes all cells without error when cleaned_load.parquet is available
    - Notebook 05 proves all pipeline layers connect (data → forecast → trade → P&L)
    - All notebooks use %matplotlib inline, plt.tight_layout(), and have markdown explanations before code cells
    - Notebook 05 includes the Pitfall 5 explanation ("Why This Notebook Exists") in markdown
  </acceptance_criteria>
  <done>Three notebooks committed. End-to-end baseline proves the full data→predict→trade pipeline connects with a rendered cumulative P&L chart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 HTTPS → data_loader.py | Untrusted external data source (PUDL Parquet files via AWS S3 public bucket) |
| pip install → requirements.txt | External package registry (PyPI) — dependency supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pipeline/data_loader.py — PUDL S3 URL | mitigate | Pin exact HTTPS URL with version tag (v2026.5.0) in module constant; document Zenodo DOI (10.5281/zenodo.20275549) as fallback verification source; do NOT accept user-supplied URLs |
| T-01-02 | Tampering | requirements.txt — pip dependency resolution | mitigate | Pin all 9 packages to exact versions with `==` operator; all versions verified via `pip index versions` on PyPI on 2026-05-20 (see RESEARCH.md §Package Legitimacy Audit) |
| T-01-03 | Denial of Service | pipeline/data_loader.py — large Parquet read | mitigate | Filter PJM balancing_authority_code immediately after load (reduces ~200MB table to ~5MB); use pandas chunked read if memory is constrained; print row counts for user visibility |
| T-01-SC | Tampering | pip install (supply chain) | mitigate | Packages tagged [ASSUMED] in RESEARCH.md §Package Legitimacy Audit. Before `pip install -r requirements.txt` executes, user must verify each package at pypi.org/project/{name}/ — this is gated by a checkpoint:human-verify created dynamically at install time (not pre-planned in this task list). Fallback policy: all 9 packages treated as [ASSUMED] until verified. |
</threat_model>

<verification>
1. Run `./setup.sh` on a clean Python 3.11 machine — verify all 9 packages install and import
2. Execute 01_data_ingestion.ipynb end-to-end — verify PUDL data downloads and displays
3. Execute 02_data_cleaning.ipynb end-to-end — verify cleaned_load.parquet is created with correct schema
4. Execute 05_end_to_end_baseline.ipynb end-to-end — verify cumulative P&L chart renders
5. Verify `data/processed/cleaned_load.parquet` exists and has columns: timestamp (datetime64[ns, UTC]), load_mw (float64), subregion (str)
</verification>

<success_criteria>
- [ ] setup.sh runs and installs all dependencies in <30 minutes on clean Python 3.11
- [ ] PUDL PJM demand data downloads successfully via HTTPS (no AWS credentials needed)
- [ ] cleaned_load.parquet is produced with UTC timestamps, load_mw values, and no NaN load rows
- [ ] Persistence forecast runs end-to-end: data → shift(24) → P&L calculation → cumulative chart rendered
- [ ] All three notebooks execute all cells without errors
- [ ] The full pipeline (download → clean → baseline forecast → P&L) is proven to connect
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md` when done
</output>
